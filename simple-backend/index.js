const express = require("express"); const app = express(); app.get("/", (req, res) => { res.json({ status: "ok", message: "Server is running" }); }); app.listen(4000, () => console.log("Server running on port 4000"));
